services:
  registry_listener:
    image: dtorry/registry_listener
    build:
      context: ../listener
      dockerfile: ../docker/ruby//Dockerfile
      cache_from:
        - type=gha,scope=registry_listener
      cache_to:
        - type=gha,scope=registry_listener,mode=max
